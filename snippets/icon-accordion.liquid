{%- if icon != 'none' -%}
  {%- assign file = icon | replace: '_', '-' | prepend: 'icon-' | append: '.svg' -%}
  <span class="svg-wrapper {{ block.settings.custom_icon_size }}">{{ file | inline_asset_content }}</span>
{%- endif -%}

{%- assign min = 1 -%}
{%- assign max = 6 -%}
{%- assign diff = max | minus: min -%}
{%- assign randomNumber = 'now' | date: '%N' | modulo: diff | plus: min -%}
{%- assign placeholder_image = placeholder | append: randomNumber | placeholder_svg_tag: 'placeholder-svg' -%}
{{ placeholder_image }}
